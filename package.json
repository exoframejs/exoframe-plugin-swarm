{
  "name": "exoframe-plugin-swarm",
  "version": "1.0.0",
  "description": "Exoframe plugin that adds Docker Swarm support",
  "main": "index.js",
  "author": "Tim Ermilov <yamalight@gmail.com>",
  "license": "MIT",
  "scripts": {
    "test": "NODE_ENV=testing jest --runInBand --coverage --silent --ci",
    "coveralls": "cat ./coverage/lcov.info | coveralls"
  },
  "devDependencies": {
    "coveralls": "^3.0.2",
    "dockerode": "^2.5.8",
    "exoframe-server": "^3.9.0-dev",
    "get-port": "^4.1.0",
    "jest": "^24.1.0",
    "rimraf": "^2.6.3",
    "tar-fs": "^2.0.0"
  }
}
